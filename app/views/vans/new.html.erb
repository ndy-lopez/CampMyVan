

<h1>Add a new listing, fuel a new dream.</h1>

<%= simple_form_for(@van) do |f| %>

  <div class='form-inputs'>
  <%= f.input :brand %>
  <%= f.input :model %>
  <%= f.input :price %>
  </div>

  <div class='form-actions'>
    <%= f.button :submit %>
  </div>
  <% end %>
