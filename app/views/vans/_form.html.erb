<%= simple_form_for(@van) do |f| %>
  <!-- [...] -->

  <div class="form-inputs">
    <%= f.input :brand %>
    <%= f.input :model %>
    <%= f.input :price %>
    <%= f.input :address %>
    <%= f.input :photo, as: :file %>
  </div>

  <div class="form-actions">
  <%= f.button :submit %>
  </div>
  <!-- [...] -->
<% end %>
