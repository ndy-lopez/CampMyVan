<%# Create a form with two inputs accept/decline
send patch request to update accept
each button will be assosciate to param true or false
in controller we will update boolean %>


  <%=  button_to 'Accept', method: 'patch', params:{accept:1} %>
  <%=  button_to 'Reject', method: 'patch', params:{accept:0} %>
